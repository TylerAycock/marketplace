import Modal from "./Modal";

const Cart = (props) => {
    return (
  <Modal>
    <h2>Shopping Cart</h2>
    <p>item name</p>
    <p>total</p>
    <button>Purchase</button>
    <button>Keep Shopping</button>
  </Modal>
    )
};

export default Cart;
